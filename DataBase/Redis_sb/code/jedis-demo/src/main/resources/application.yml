spring:
  redis:
    port: 6379 #端口号
    password: #密码
    host: #ip
    jedis:
      pool:
        max-idle: 6    #最大空闲数
        max-active: 10 #最大连接数
        min-idle: 2    #最小空闲数
    timeout: 2000   #连接超时